# Open House — PRD

## Original Problem Statement
Build a sleek, laid-back, editorial-style website for "Open House" — an independent online radio station and cultural platform. Warm minimalism: off-white background, charcoal text, muted sage accent, oversized editorial typography, candid imagery.

## Current Scope (pivoted 2026-08-28)
Simplified to a SINGLE-PAGE launch/landing page. No backend, no database, no REACT_APP_BACKEND_URL — standalone static frontend deployable to Vercel as-is. All fake/example shows, schedules, projects and multi-page structure removed to avoid looking more established than the brand currently is.

## Live Brand Details (confirmed by user)
- Instagram: https://www.instagram.com/openhouse_radio
- Contact email: david@openhouseradio.co.uk (mailto CTAs)
- Radio.co stream: https://streams.radio.co/seb9792770/listen (verified HTTP 200, wired to a real HTML5 audio player)

## Architecture
- Frontend only: React 19 + framer-motion + lenis + Tailwind (no router, no axios, no API calls)
- Design system unchanged: Outfit / Manrope / Newsreader; #F9F8F6 paper, #1C1C1A ink, #A3B19B sage, #252523 coal; noise overlay, editorial borders
- Player: global context (`src/lib/player.jsx`) drives a real `Audio` element on the Radio.co stream; shared by nav button, hero CTA, listen section and bottom player bar (play/pause, live dot, equalizer animation)
- Page sections: Hero (masked OPEN HOUSE reveal + parallax image + On Air badge) → editorial marquee → Listen Live (dark player module) → Get Involved (host-a-show invite, mailto + Instagram CTAs) → contact strip → footer (Instagram + email + copyright)
- .gitignore (root + frontend) now ignores all .env files

## Implemented
- 2026-08-28: Full 6-page editorial site with FastAPI/MongoDB backend, submissions + Resend email notifications
- 2026-08-28: Submission notifications routed to owner's inbox
- 2026-08-28: Simplified to standalone single-page landing; real Radio.co stream wired into player; real Instagram and contact email; .env gitignored; backend/multi-page code removed from frontend bundle
- 2026-08-28: Now Playing — Radio.co public API (public.radio.co/api/v2/seb9792770: track/current + status + source, no key, CORS *) polled every 30s in player context; Listen section shows artwork + track/artist + live DJ name when on air, graceful "Currently between broadcasts" when off air; player bar shows live track line
- 2026-08-28: Imagery restyled per user feedback — replaced club/electronic visuals with warm, natural, artistic photos (hero: vinyl spines + headphones in daylight; listen section: piano keys in soft light). Now Playing confirmed live on air (Michael Franks track with artwork)
- 2026-08-28: ART DIRECTION PIVOT — user supplied hand-drawn house logo ("open house" in a crayon house outline, terracotta chimney/sun on cream #FDF8EF). Photography removed in favour of illustration: the mark is the hero art (saved at frontend/public/open-house-mark.png, bg matches page paper exactly); custom hand-drawn SVG doodles (radio with terracotta waves on Listen section, open door with terracotta sun on Get Involved) in components/Doodle.jsx; accent shifted sage→terracotta clay (#C67656 / #A85D40), paper warmed to #FDF8EF. Now Playing re-confirmed live (Greg Henderson)
- 2026-08-28: Favicon from the house mark — tight-cropped icon set (favicon.ico 16+32, favicon-16/32.png, apple-touch-icon 180, icon-192/512) in frontend/public; index.html title "Open House — Independent Radio & Culture", meta description, theme-color #FDF8EF, icon links
- 2026-08-28: Handwritten wordmark — cropped the "open house" text from the user's drawing, background made transparent (frontend/public/open-house-wordmark.png), now used in nav (with hover tilt) and footer. Added Discord (discord.gg/AGsudUcFn) and WhatsApp community links: icon links in nav, text links in Get Involved ("join the conversation") and footer "Say hello" list
- 2026-08-29: Community section ("Come as you are.") — two large editorial cards for Discord + WhatsApp between Get Involved and contact strip. Schedule section ("This Week") added after Listen: hand-drawn CalendarDoodle placeholder now; Radio.co schedule widget is WIRED but needs the widget ID — set SCHEDULE_WIDGET_ID in App.js (Radio.co dashboard → Widgets → Schedule → Share; station ID alone returns 404, the per-widget ID is required)
- 2026-08-29: WhatsApp preview debug — verified server-side that tags + image serve correctly to WhatsApp's crawler UA and via independent scraper (microlink extracts title/desc/image successfully). Added og:image:secure_url. Root cause of "no logo" is WhatsApp's URL preview cache (link was shared before tags existed). Fix for user: share URL with a cache-buster e.g. ?v=2. Reminder: repoint og:image + og:url to openhouseradio.co.uk at domain launch
- 2026-08-29: BUGFIX — schedule rows showed "Default" (the name of Radio.co's default playlist — that's all the embed/schedule API exposes for playlist-based slots). Rows labelled Default now render as "Open House Radio"; redundant "Open House" artist suffix suppressed; when a DJ connects live, their Radio.co DJ name appears on the ON AIR badge (via /source). USER GUIDANCE: to show show/DJ names, schedule named playlists or named events in Radio.co instead of the Default playlist — names flow through automatically
- 2026-09-06: Hero mark enlarged — tight-cropped version (open-house-mark-tight.png, content bbox + small padding) replaces full-canvas mark, max-w bumped to xl so the drawing matches the headline block's presence. Schedule page intro paragraph removed per user
- 2026-09-06: SCHEDULE EXPERIENCE — homepage "This Week" big list replaced directly after Listen section: up to 3 upcoming programmes (live event first with LIVE NOW label) as listings columns (day/date, time range, big title, serif host), "View Full Schedule →" link, CalendarDoodle empty state. New /schedule route (react-router reintroduced, Landing extracted, ScrollToTop added) — listings page styled as radio programme guide: masked SCHEDULE title, day-grouped entries with horizontal rules, time/title/host columns, ON AIR badges. Nav gains "Schedule" text link; logo now routes home. Radio.co feed integration unchanged (shared player context)
- 2026-09-06: DJ names in schedule — eventParts() helper extracts host from ev.dj.name (DJ-slot events), playlist.artist (when not "Open House"/Default), or "Show — DJ" title pattern; host shows in schedule rows, next-on entries and up-next strip. RadioDoodle viewBox given headroom (0 -16 400 336) fixing clipped top broadcast wave
- 2026-09-06: MAKE IT FEEL LIVE — real metadata layer: (1) schedule fetch moved into shared player context (polled 5 min, helpers exported: fmtDay/fmtDate/fmtTime/isLiveEvent/upcomingEvents/eventTitle); (2) hero On Air badge is now dynamic — red pulsing dot + current track title when live ("LIVE NOW · Everybody Loves The Sunshine" verified), "Off Air / Back soon" when not; (3) hero micro-meta "Live from London — Est. 2026"; (4) Listen section up-next strip — "LIVE NOW · title · until HH:MM" or "UP NEXT · title · MON 07 SEPT · 18:00—19:00" from real schedule; (5) This Week first row carries terracotta "UP NEXT" label; (6) nav Listen Live dot now reflects real station status (red pulsing when on air). All real data, no fake content
- 2026-08-29: Google search favicon — added Google-compliant icon sizes (48×48 and 192×192 PNG, multiples of 48px per Google's favicon spec) to index.html link tags. ROOT CAUSE of no Google listing: the Emergent preview ingress sends `x-robots-tag: noindex, nofollow`, so Google is forbidden from indexing the preview URL entirely. Resolution requires the site live on openhouseradio.co.uk (Vercel sends no such header) + Google Search Console submission; favicon then appears in results after Google's recrawl (days–weeks)
- 2026-08-29: Hero alignment fix — house mark no longer uses the absolute/130%-bleed hack; it's a flex-centred grid column vertically aligned against the text block, On Air badge tucked beneath it
- 2026-08-29: Hero tagline final per user — single terracotta serif line: "Open House is open." (the other two lines were removed at user request); passions list kept below; footer line ends with "Open House is open."
- 2026-08-29: COPY REFINEMENT (copy-only, no design changes) Hero: kicker "Independent online radio", serif line + new list-of-passions paragraph, CTA "Have a Show". Get Involved: "Anyone can have a show." + "No studio. No fancy setup... A laptop, a mic and your bedroom will do nicely." + "Pitch a Show" button. Marquees: "Shows About Anything", "No Experience Needed", "A Laptop & a Mic", "Broadcast From Your Bedroom". Contact strip quote: "If you care about something, there's probably a show in it." — kettle line demoted to small easter egg ("ps — the kettle is boiling"). Footer description updated. Banned brand-speak avoided (creative platform, curated experiences etc.)
- 2026-08-29: BUGFIX + REDESIGN — user saw duplicate schedules (transient second iframe from the script-injection approach) and preferred the custom design. Widget iframe removed entirely; This Week is now a fully custom on-brand schedule list fed by Radio.co's public schedule endpoint (https://public.radio.co/stations/seb9792770/embed/schedule, CORS *, JSON: data[] with start/end/event_id/playlist{name,artist,title}), fetched on load + every 5 min. Rows show day, date, time range, title, artist, terracotta ON AIR badge when live; CalendarDoodle placeholder when empty. Blank-page regression caused by missing useState import fixed same session

## Backlog / Next Tasks
- P1: Deploy to Vercel + custom domain openhouseradio.co.uk (guide delivered to user 2026-08-28; user-side: GitHub push, Vercel import with root=frontend, DNS A/CNAME records)
- P2: Grow back into multi-page site (Shows, Projects, About, Submit) when the brand is ready — the previous components are in git history
- P2: Newsletter signup
